

//=============================ALTER TABLE company_profile ADD is_allow_negative BOOLEAN NOT NULL AFTER is_office_payment_open;
ALTER TABLE `billing_reading` ADD `is_send_csv` BOOLEAN NOT NULL AFTER `back_table_pkey`, ADD `is_direct_to_bank` BOOLEAN NOT NULL AFTER `is_send_csv`;

ALTER TABLE billing_customer_info ADD complete_deleted VARCHAR(20) NULL AFTER status;
ALTER TABLE billing_customer_info ADD complete_deleted_date DATETIME NULL AFTER complete_deleted;

ALTER TABLE user_account ADD is_allow_previous_reading BOOLEAN NOT NULL AFTER modified_date;
ALTER TABLE billing_customer_info ADD kdme_kfya_reasons TEXT NULL AFTER complete_deleted_date;
ALTER TABLE billing_reading ADD bill_description_bank TEXT NULL AFTER is_direct_to_bank;


i do deploy this spring boot with next js project on ubuntu os as followos check to meet the following criteria and if some wrong thing recomend best way  
1. backend war created with mvnd -DskipTests clean package
2. backend run with the following service
[Unit]
Description=WBill Backend Spring Boot Application
After=network.target mysql.service

[Service]
Type=simple
User=woldia
WorkingDirectory=/home/woldia/Documents/systemDataDontDelete2
ExecStart=/usr/bin/java -jar /home/woldia/Documents/systemDataDontDelete2/wbill.war
Restart=always
RestartSec=10
Environment=SPRING_PROFILES_ACTIVE=prod

[Install]
WantedBy=multi-user.target

3. the system is hosted on cloud so it havel local ip 172.10.10.64 and public ip 196.190.220.35 
4. i want to run next js front end with pm2 and next js to manage the  forwar request to the backed since the backedn is not acsesable the the public
5. i want the backed to be acsessed via next js and run on localhost 


sudo chown woldia:woldia wbill.war
sudo chmod 755 wbill.war

mvn clean package
mvnd  clean package

mvn -DskipTests clean package

sudo systemctl stop wbill-backend.service
sudo nano  /etc/systemd/system/wbill-backend.service 

sudo systemctl daemon-reload
sudo systemctl enable wbill-backend.service
sudo systemctl restart wbill-backend.service

sudo systemctl start wbill-backend.service
sudo systemctl status wbill-backend.service
sudo systemctl stop wbill-backend.service

=====pm2=======
pm2 start npm --name wbill-frontend -- start
sudo pm2 start wbill-frontend ecosystem.config.js --env production
# Persist across reboots
pm2 save

pm2 status
pm2 logs wbill-frontend --lines 200

Pm2 stop wbill-frontend


===========


==== wbill-backend.service  ==
[Unit]
Description=WBill Backend Spring Boot Application
After=network.target mysql.service

[Service]
Type=simple
User=woldia
WorkingDirectory=/home/woldia/Documents/systemDataDontDelete2
ExecStart=/usr/bin/java -jar /home/woldia/Documents/systemDataDontDelete2/wbill.war
Restart=always
RestartSec=10
Environment=SPRING_PROFILES_ACTIVE=prod

[Install]
WantedBy=multi-user.target


===================application-prod =====

########################################
# JWT / Security
########################################
school.app.jwtSecret= ======================PROD=Spring===========================
school.app.jwtExpirationMs=8600000
school.app.jwtRefreshExpirationMs=86400000

########################################
# DataSource (MySQL on Ubuntu)
########################################
# Example: MySQL running on same Ubuntu server, default port 3306
# Change database name, user and password as needed.
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/wbill_jawi_tkmt?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root2
spring.datasource.password=Fb@45412921

# HikariCP pool settings
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.pool-name=WbillHikariPool
spring.datasource.hikari.shutdown-timeout=5000

########################################
# JPA / Hibernate
########################################
# Use 'none' if schema is managed manually; 'update' if you want Hibernate to auto-update.
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# JPA batch settings (keep as in dev unless you have other needs)
spring.jpa.properties.hibernate.jdbc.batch_size=50
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.batch_versioned_data=true

########################################
# Server
########################################
# Port your backend will listen on in production
server.port=8082
# Bind to all interfaces so it can be reached externally
server.address=0.0.0.0

########################################
# Jackson / Serialization
##############


================= .env.local ========

BACKEND_URL=http://localhost:8082
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=d8cb6a6db1fa60686eedc85c857d6fc7394fd36803f296e4f019e11ac869a348
============================



sudo adduser --system --group --home /opt/wbill_frontend wbillfe

/opt/wbill_frontend/Wbill_FT_23

sudo chown -R wbillfe:wbillfe /opt/wbill_frontend

sudo mkdir -p /etc/wbill

sudo nano /etc/wbill/frontend.env
cd /opt/wbill_frontend/Wbill_FT_23
sudo npm i -force
     sudo npm ci
sudo NODE_ENV=production npm run build



cd /opt/wbill_frontend/Wbill_FT_23/deploy
sudo pm2 start ecosystem.config.js --env production
pm2 status
pm2 logs wbill-frontend

sudo pm2 stop wbill-frontend

pm2 save
pm2 startup systemd

sudo ufw allow 3000/tcp

curl -i http://127.0.0.1:8082/api/card_managenment/customer/all -H "Authorization: Bearer "

{"name":"Wondale Alebachew Yerdaw","sub":"67",
"accessToken":"
eyJhbGciOiJIUzI1NiJ9.eyJzdWIiOiJyZWFkaW5nIiwiaWF0IjoxNzYzMzEyMjQxLCJleHAiOjE3NjMzMjA4NDF9.G5rI05HobjXcy4IVrUjFUJRDIINjR0qcrJByNs_DAfw

",

"id":67,"username":"reading","roles":["billzgjt"],"iat":1763312241,"exp":1765904241,"jti":"77ce4b0b-af18-4625-85c9-e307e5f691c5",
"isTokenExpierd":0,"user":{"roles":["billzgjt"],"role":"billzgjt"}}


curl -i http://127.0.0.1:8082/api/card_managenment/customer/all ^-H "Authorization: Bearer <eyJhbGciOiJIUzI1NiJ9.eyJzdWIiOiJyZWFkaW5nIiwiaWF0IjoxNzYzMzEyMjQxLCJleHAiOjE3NjMzMjA4NDF9.G5rI05HobjXcy4IVrUjFUJRDIINjR0qcrJByNs_DAfw>"
  
  curl -i http://127.0.0.1:3000/backend/api/card_managenment/customer/all ^-H "Authorization: Bearer <eyJhbGciOiJIUzI1NiJ9.eyJzdWIiOiJyZWFkaW5nIiwiaWF0IjoxNzYzMzEyMjQxLCJleHAiOjE3NjMzMjA4NDF9.G5rI05HobjXcy4IVrUjFUJRDIINjR0qcrJByNs_DAfw>"
  
  curl -i "http://127.0.0.1:8082/api/card_managenment/customer/customers/paginated/active?page=0&size=10" ^
  -H "Authorization: Bearer <TOKEN>"
  
  curl -i "http://127.0.0.1:8082/api/card_managenment/customer/customers/paginated/active?page=0&size=10" ^
  -H "Authorization: Bearer <TOKEN>"